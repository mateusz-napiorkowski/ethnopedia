# CKC server backup

This branch preserves the deployment-specific changes found on the CKC server
(`/home/wkasperski/ethnopedia`) before the PBI integration work.

Only small tracked configuration/script changes are included. Runtime data
directories such as `mongodb_data/` and `backups/` are intentionally not
committed.
